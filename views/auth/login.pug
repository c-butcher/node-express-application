extends ../layout

block content
    div(class="col-lg-6 offset-lg-3 col-md-8 offset-md-2 col-sm-12 text-center")
        form(class="form px-3 py-4 m-3 border shadow-sm rounded-lg" method="POST")
            h3(class="mb-0 font-weight-normal") Please sign in

            nav(class="col-lg-12 btn-group my-4")
                a(class="btn btn-outline-primary" href="/auth/google") Google
                a(class="btn btn-outline-primary" href="/auth/facebook") Facebook
                a(class="btn btn-outline-primary" href="/auth/twitter") Twitter

            label(for="inputEmail" class="sr-only") Email address
            input(type="text" id="inputEmail" class="form-control mb-4" name="email" placeholder="Username / Email Address" required="")

            label(for="inputPassword" class="sr-only") Password
            input(type="password" id="inputPassword" class="form-control mb-4" name="password" placeholder="Password" required="")

            p(class="text-center")
                | Not a member? You can always <a class="text-center" href="/signup">register a new account</a>.

            button(class="btn btn-lg btn-primary btn-block" type="submit") Sign in